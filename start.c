#include start.h

void print_nl(stringdata)
{
   print_new_line()
   print_string(stringdata)
}

// COMPLEX 5C COMMAND LINE

int cake_base_five()
{
  char ch = 0;
  char cha = 0;
  char chb = 0;
  char keycode = 0;
  do{
    keycode = get_input_keycode();
    if(keycode == KEY_ENTER){
      print_new_line();
    }else{
      ch = get_ascii_char(keycode);
      print_char(ch);
      do{
        keycode = get_input_keycode();
        if(keycode == KEY_ENTER){
           print_new_line();
        }else{
          cha = get_ascii_char(keycode);
          print_char(cha);
          do{
            keycode = get_input_keycode();
            if(keycode == KEY_ENTER){
               print_new_line();
            }else{
              cha = get_ascii_char(keycode);
              print_char(cha);
            }
            sleep(0x02FFFFFF);
            }while(ch > 0);
        }
        sleep(0x02FFFFFF);
         }while(ch > 0);
    }
    sleep(0x02FFFFFF);
    }while(ch > 0);
  // CHK COMMAND
  if(ch == "C")
    if(ch == "H") {
            if(ch == "K") {
                return 1;
            }
    }
  }


// COMPLEX 5C COMMAND LINE


int cake_base_three()
{
  char ch = 0;
  char cha = 0;
  char chb = 0;
  char keycode = 0;
  do{
    keycode = get_input_keycode();
    if(keycode == KEY_ENTER){
      print_new_line();
    }else{
      ch = get_ascii_char(keycode);
      print_char(ch);
      do{
        keycode = get_input_keycode();
        if(keycode == KEY_ENTER){
           print_new_line();
        }else{
          cha = get_ascii_char(keycode);
          print_char(cha);
          do{
            keycode = get_input_keycode();
            if(keycode == KEY_ENTER){
               print_new_line();
            }else{
              cha = get_ascii_char(keycode);
              print_char(cha);
            }
            sleep(0x02FFFFFF);
            }while(ch > 0);
        }
        sleep(0x02FFFFFF);
         }while(ch > 0);
    }
    sleep(0x02FFFFFF);
    }while(ch > 0);
  // CHK COMMAND
  if(ch == "C")
    if(ch == "H") {
            if(ch == "K") {
                return 1;
            }
    }
  }
             // FIX BUGS tho i think they fixed

void setup_db()
{
   print_new_line();
   init_vga(GREEN, BLACK);
   print_new_line();
   print_string("DogeBakery Compiler Version One");
   print_new_line();
   print_string("RUNNING THREE C CONSOLE...");
   print_new_line();
   print_string("THIS HAS ALL COMMANDS WITH THREE CHs SWITCH WITH SWT");
   char ext_booleam = 0;
   char chmode = 0;
   // 3 C TERMINAL
   do{
           print_string("THIS HAS ALL COMMANDS WITH THREE CHs SWITCH WITH SWT");
           if(cake_base_three == 1) {
              print_string("RUNNING FIVE C CONSOLE...");
              print_new_line();
              print_string("THIS HAS ALL COMMANDS WITH FIVE CHs SWITCH WITH SWICH");
              chmode = 1;
           }

   }while(ch == 0)
   // FIVE C TERMINAL -- COMPLETE SOON
   do{

   }while(ch == 1)


}
