#ifndef START_H
#define START_H


// START CODING SYSTEM
#include <stdio.h>


void print_nl(stringdata)
{
   print_new_line()
   print_string(stringdata)
}

// COMPLEX 5C COMMAND LINE

void if_compression()
{
  do {
    char ch = 0;
    char cha = 0;
    char chb = 0;
    char chc = 0;
    char chd = 0;
    char input = 0;
    char gape = 0;
    char keycode = 0;
    char added = 0;
    char mape = 0;
    char sape = 0;
    char cape = 0;
    do{
            keycode = get_input_keycode();
            if(keycode == KEY_ENTER){
                    print_new_line();
            }else{
                    chd = get_ascii_char(keycode);
                    print_char(input);
                    if(input != "]"){
                        gape = strcat(input,gape);
            }
            }

            sleep(0x02FFFFFF);
    }while(input != "]");
    ch = gape[0 + added];
    cha = gape[1 + added];
    chb = gape[2 + added];
    chc = gape[3 + added];
    chd = gape[4 + added];

    if(ch == "P") {
        if(cha == "R") {
            if(chb == "N")
            {
                if(chb == "T")
                {
                    if(chb == ".") {
                        print_new_line()
                        print_string(mape)

            }
        }

            }
    }
  }

    if(ch == "A") {
        if(cha == "R") {
            if(chb == "T")
        {
                if(chb == ".")
            {
                    if(chb == "C") {
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)
                        print_new_line()
                        print_string(mape)

            }
        }

            }
    }
  }

        if(ch == "I") {
            if(cha == "F") {
                if(chb == ".")
        {
                    if(chb == "A")
            {
                        if(chb == ".") {
                            char cape = mape;


            }
        }

            }
  }

}
    if(ch == "I") {
        if(cha == "F") {
            if(chb == ".")
            {
                if(chb == "B")
                {
                    if(chb == ".") {
                        char sape = cape;
                        if(sape == cape) {
                            if_compression
                        }

            }
        }

            }
  }

}
    added = added + 5;
}while exit != 1;
  }


void cake_base_five_give()
{
  char ch = 0;
  char cha = 0;
  char chb = 0;
  char chc = 0;
  char chd = 0;
  char input = 0;
  char gape = 0;
  char keycode = 0;
  do{
    keycode = get_input_keycode();
    if(keycode == KEY_ENTER){
      print_new_line();
    }else{
      ch = get_ascii_char(keycode);
      print_char(ch);
      do{
        keycode = get_input_keycode();
        if(keycode == KEY_ENTER){
          print_new_line();
        }else{
          cha = get_ascii_char(keycode);
          print_char(cha);
          do{
            keycode = get_input_keycode();
            if(keycode == KEY_ENTER){
              print_new_line();
            }else{
              chb = get_ascii_char(keycode);
              print_char(chb);
              do{
                keycode = get_input_keycode();
                if(keycode == KEY_ENTER){
                    print_new_line();
                }else{
                    chc = get_ascii_char(keycode);
                    print_char(chc);
                    do{
                      keycode = get_input_keycode();
                      if(keycode == KEY_ENTER){
                            print_new_line();
                      }else{
                        chd = get_ascii_char(keycode);
                        print_char(chd);
                        // INPUT
                        do{
                            keycode = get_input_keycode();
                            if(keycode == KEY_ENTER){
                                print_new_line();
                            }else{
                                chd = get_ascii_char(keycode);
                                print_char(input);
                                if(input != ";"){
                                    gape = strcat(input,gape);
                                }
                            }

                            sleep(0x02FFFFFF);
                            }while(input != ";");
                            // INPUT END
                        }

                        sleep(0x02FFFFFF);
                        }while(chd > 0);
                        }

                    sleep(0x02FFFFFF);
                    }while(chc > 0);
                    }
                    sleep(0x02FFFFFF);
                    }while(chb > 0);
                }
         sleep(0x02FFFFFF);
         }while(cha > 0);
    }
    sleep(0x02FFFFFF);
    }while(ch > 0);
  // COMMANDS
  if(ch == "P") {
    if(cha == "R") {
        if(chb == "N")
        {
            if(chb == "T")
            {
                if(chb == ".") {
                        print_new_line()
                        print_string(gape)

            }
        }

            }
    }
  }

  if(ch == "A") {
    if(cha == "R") {
        if(chb == "T")
        {
            if(chb == ".")
            {
                if(chb == "C") {
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)
                        print_new_line()
                        print_string(gape)

            }
        }

            }
    }
  }

    if(ch == "I") {
     if(cha == "F") {
        if(chb == ".")
        {
            if(chb == "A")
            {
                if(chb == ".") {
                        char mape = gape;


            }
        }

            }
  }

}
  if(ch == "I") {
   if(cha == "F") {
        if(chb == ".")
        {
            if(chb == "B")
            {
                if(chb == ".") {
                        char sape = gape;
                        if(sape == mape) {
                            execute_if()
                        }

            }
        }

            }
  }

}





// COMPLEX 5C COMMAND LINE


int cake_base_three()
{
  char ch = 0;
  char cha = 0;
  char chb = 0;
  char keycode = 0;
  do{
    keycode = get_input_keycode();
    if(keycode == KEY_ENTER){
      print_new_line();
    }else{
      ch = get_ascii_char(keycode);
      print_char(ch);
      do{
        keycode = get_input_keycode();
        if(keycode == KEY_ENTER){
           print_new_line();
        }else{
          cha = get_ascii_char(keycode);
          print_char(cha);
          do{
            keycode = get_input_keycode();
            if(keycode == KEY_ENTER){
               print_new_line();
            }else{
              chb = get_ascii_char(keycode);
              print_char(chb);
            }
            sleep(0x02FFFFFF);
            }while(ch > 0);
        }
        sleep(0x02FFFFFF);
         }while(ch > 0);
    }
    sleep(0x02FFFFFF);
    }while(ch > 0);

  // CHK COMMAND
  if(ch == "C") {
    if(cha == "H") {
            if(chb == "K") {
                return 1;
            }
    }
  }
  }
             // FIX BUGS tho i think they fixed

void setup_db()
{
   print_new_line();
   init_vga(GREEN, BLACK);
   print_new_line();
   print_string("DogeBakery Compiler Version One");
   print_new_line();
   print_string("RUNNING THREE C CONSOLE...");
   print_new_line();
   print_string("THIS HAS ALL COMMANDS WITH THREE CHs SWITCH WITH SWT");
   char ext_booleam = 0;
   char chmode = 0;
   // 3 C TERMINAL
   do{
           print_string("THIS HAS ALL COMMANDS WITH THREE CHs SWITCH WITH SWT");
           if(cake_base_three == 1) {
              print_string("RUNNING FIVE C CONSOLE...");
              print_new_line();
              print_string("THIS HAS ALL COMMANDS WITH FIVE CHs SWITCH WITH SWICH");
              chmode = 1;
           }

   }while(ch == 0)
   // FIVE C TERMINAL -- COMPLETE SOON
   do{

   }while(ch == 1)


}



#endef
